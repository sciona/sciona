# Supplemental execution profile for the qualified native regression lifecycle.
# Install alongside the source-declared sciona/sciona-atoms-ml requirements.
# This pins the qualified backend; it is not a full environment lockfile.
catboost==1.2.10
